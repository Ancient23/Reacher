# Roadmap build journal

Append-only. One line per completed unit or human gate, newest at the bottom. A fresh agent (or the
orchestrator) can read this for history without opening code or diffs. Format:

`YYYY-MM-DD · U<id> · <CONTINUE|HUMAN_GATE|COMPLETE|FAILED> · <one-line what happened / what's needed>`

---

<!-- entries below -->
2026-06-27 · U1 · CONTINUE · Fleet config schema+loader (pydantic): reachy_fleet_supervisor.fleet — FleetConfig/ProjectConfig/GatePolicy/McpServerConfig + load_fleet_config (toml/json, strict). fleet.example.toml + 24 tests green. (Pre-existing unrelated failures: torch-less vision tests, 2 profile-loading tests.) Next: U2 FleetManager over `claude --bg`.
2026-06-28 · U2 · CONTINUE · FleetManager over `claude --bg`: fleet/manager.py — build_spawn_argv/parse_spawn_output/parse_agents_json (pure) + FleetManager.spawn/info/is_running/stop/rm/stop_and_remove + AgentInfo + list_agents. Short id from stdout is authoritative; real sessionId resolved from `claude agents --json`. 16 tests green incl. a REAL bg-session lifecycle test (spawn→assert→stop+rm, cleaned up). (Recovered: prior iteration left this work uncommitted.) Next: U3 SessionManager (track N + reconnect).
2026-06-28 · U3 · CONTINUE · SessionManager (track N + reconnect): fleet/session_manager.py — registry keyed by short id, lookup by id-or-name (ambiguous-name guard), spawn (dup-name reject) / adopt / forget / stop / stop_and_remove(_all). reconnect() + from_roster() re-adopt background-only rows from `claude agents --json` (predicate-scoped, idempotent); FleetManager.from_agent_info() added as the reconnect builder. 17 new tests green incl. a REAL reconnect-after-restart test (spawn → fresh SessionManager re-adopts from live roster → cleaned up). Fleet suite 57 green. Next: U4 FleetState model + poller (pub/sub).
2026-06-28 · U4 · CONTINUE · FleetState model + poller (pub/sub): fleet/state.py — ManagerSnapshot/FleetSnapshot (frozen, value-comparable; needs_attention, last_line, lookup by id/session/name) + FleetState (thread-safe single source of truth: apply() folds a poll, merges per-manager transcript ring buffers via overlap-dedup, notifies subscribers ONLY on change; subscribe/unsubscribe, fire_immediately, bad-subscriber isolation) + tail_logs (best-effort `claude logs <id>`) + FleetPoller (background thread, injectable agents_source/logs_source, predicate scope, error-tolerant loop, ctx-mgr). 25 new tests green incl. a REAL poll-populates-FleetState integration test (spawn bg → real poll lands it + fires subs → cleaned up; 0 stray agents). Fleet suite 82 green. Next: U5 status convention (manager emits, fleet reads) — decision #21; will extend FleetState.apply to merge each manager's own status.json/report line.
2026-06-28 · U5 · CONTINUE · Status convention (manager emits, fleet reads) — decision #21: fleet/status.py — ManagerStatus (frozen; sentinel state CONTINUE/HUMAN_GATE/COMPLETE/FAILED/RUNNING + summary/unit/phase/iteration/gate/extra; updated_at compare=False so no-op re-emit doesn't fire subs; needs_attention/is_terminal; to/from_dict folds unknown keys into extra; from_report_line/parse_sentinel) + known-place convention (<status_dir>/<name>/status.json; default ~/.reachy_fleet/status or $REACHY_FLEET_STATUS_DIR) + atomic write_status + best-effort read_status/read_status_for/read_statuses_for_agents (name->fleet-key mapping). Wired into state.py: ManagerSnapshot.report + headline + needs_attention merges emitted status; FleetState.apply(statuses=…); FleetPoller status_dir/statuses_source. 18 new tests; fleet suite 100 green; 0 stray bg agents. Next: U6 headless `fleet` CLI.
2026-06-28 · U6 · CONTINUE · Headless `fleet` CLI: fleet/cli.py — `fleet start|spawn|status|logs|stop` over SessionManager (reconnect-from-roster, durable bg sessions) + FleetState/FleetPoller (one poll per invocation, decision #21 read-only). argparse subcommands, shared --claude-bin/--status-dir/--json; key = short id or voice name; status merges emitted status.json (needs_attention/headline); stop has --rm/--all; logs is read-only `claude logs` tail. Added `fleet` console_script (pyproject) + build_parser/cli_main exports. 20 new tests (mocked-roster logic for every command, text+JSON, error exit codes) incl. a REAL end-to-end test (spawn→status→logs→stop --rm via cli.main, cleaned up; 0 stray agents). Fleet suite 120 green. Next: U7 read-only web dashboard (FastAPI on settings_app rendering FleetState + JSON poll endpoint).
2026-06-28 · U7 · CONTINUE · Read-only web dashboard: fleet/dashboard.py — create_dashboard_app(state, *, title, poll_ms, on_request) builds a FastAPI app over FleetState: GET / (server-rendered 2–4 card grid: name/id, state badge + emitted-report state, current tool, waiting/attention flag, headline, transcript ring buffer in a <details>; plus a tiny JS poller that re-renders live from the JSON), GET /api/fleet (snapshot_payload = CLI's _snapshot_dict with transcripts, so dashboard JSON never drifts from `fleet status --json`), GET /healthz. on_request hook = poll-on-demand for a standalone server (errors swallowed; serves last snapshot). All fields HTML-escaped. Pure render_cards/render_page exported. FastAPI/starlette/httpx already present transitively via gradio (no new deps). 12 tests (TestClient HTML+JSON, status merge, escaping, attention, refresh hook) incl. a REAL bg-session end-to-end (spawn→poll→see on / and /api/fleet→stop+rm; 0 stray agents). Fleet suite 132 green; ruff+mypy clean. Next: U9 per-agent identity (name/color) + 2–4 wiring (software). NB U8 Body-renders-FleetState (hardware gate) also has deps satisfied and remains pending.
2026-06-28 · U9 · CONTINUE · Per-agent identity (name/color) + 2–4 wiring: fleet/identity.py — AgentColor(name/hex/ansi; to_dict + 256-color swatch) + 6-hue PALETTE (sized past the 2–4 target) + color_for(key) (deterministic single-key color via SHA-1, stable cross-process) + assign_colors(keys) (whole-fleet map: hash-preferred slot with collision probing; keys sorted so the map is roster-order-independent → distinct colors guaranteed while fleet ≤ palette, graceful reuse on overflow). Wired through the single source of truth: ManagerSnapshot gains a `color` field; FleetState.apply assigns colors over the full key set each poll (stable per manager across re-polls, value-equality unchanged for change-detection). Surfaced in every renderer: CLI _manager_dict adds `color` to --json + a swatch/·name tag in text (name [id] kept contiguous so old assertions hold); dashboard cards carry an `--agent` hex (left border + name dot, title=color name) server-side AND in the live-poll JS, plus `color` in /api/fleet. Exported from fleet __init__ (AgentColor/PALETTE/color_for/assign_colors). 12 new identity tests + color tests added to state/cli/dashboard suites; full fleet suite 148 green; identity.py ruff+mypy clean (pre-existing D-rule noise in older fleet files left untouched); 0 stray bg agents. Next: U8 Body renders FleetState (HARDWARE gate) — map fleet status→head yaw/antenna; deps [U4] done. Phase-2 software is now exhausted (U10 = hardware-gated integration capstone, blocked on U8).
2026-06-28 · U8 · HUMAN_GATE · Body renders FleetState (HARDWARE gate): fleet/body.py — pure status→body mapping (BodyPose in degrees; slot_yaw spreads 1–4 managers symmetrically across ±40° in key order matching U9; antenna_pose per signal idle/working/attention/done/failed with distinct magnitudes; signal_state prefers emitted report then roster row, failed>attention; choose_target = the manager most needing a human; body_signal_for faces that manager's slot + antennas=its state, front+aggregate when nobody waits) + FleetBodyRenderer (FleetState subscriber, applies only on pose change, bad-applier isolation, attach()) + make_goto_applier (deg→rad GotoQueueMove on the movement_manager; lazy reachy_mini imports so the module/tests need no motors). Exported from the fleet package. Plus fleet/body_demo.py — `uv run python -m reachy_fleet_supervisor.fleet.body_demo` drives the real robot through a scripted 4-step synthetic fleet (no live agents). 22 new tests (tests/fleet/test_body.py incl. the demo scenario); fleet suite 169 green; body.py/body_demo.py ruff+mypy clean. HUMAN_GATE: power robot on rear USB-C, run the demo, confirm the torso turns toward the flagged manager (steps 2&3 = two opposite directions) and antennas differ per state; report PASS/PROBLEM (a wrong-direction result is a one-line sign-flip in make_goto_applier). Unit stays in_progress; steps in STATE.yaml status.pending_gate. Next: on PASS, mark U8 done then U10 (Phase 2 integration capstone, also hardware-gated).
2026-06-28 · U8 · HUMAN_GATE · Body renders FleetState — FIX after reported PROBLEM ("body turns, a bit abrupt; antennas never droop on failure"). Root causes: (1) idle BreathingMove restarts ~0.3s after each goto ends and pulls antennas→neutral + forces body_yaw→0, so the droop only flashed ~1s — body_demo now HOLDS each pose via new hold_pose() which re-marks set_moving_state every 0.2s (< the 0.3s idle delay) for the step window, keeping the commanded pose on the robot; (2) make_goto_applier read current joints wrong — get_current_joint_positions() returns (head_joints[7], antennas[2]) with NO body_yaw, but code read the 2nd element as [body_yaw,al,ar] → joints[2] IndexErrors on hardware → every move started from neutral and snapped through 0° (the "abrupt"); applier now tracks the last commanded body_yaw+antennas and interpolates from there (smooth sweeps); (3) failed droop deepened -30→-45 (mirror of attention +45) and is now the ONLY state below neutral. Polish: demo --duration default 1.0→1.5 (gentler). Tests: strengthened antenna assertions (failed = sole downward droop, both antennas same sign), added applier interpolate-from-last test + 2 hold_pose tests + demo-scenario droop assertion; tests/fleet/test_body.py 25 green, fleet suite 173 green. Backlog captured: "Reachy SPEAKS the status aloud" → note added to U15 (Gate→voice escalation) acceptance/notes. Antenna droop is HARDWARE-PERCEPTUAL → cannot self-verify; HUMAN_GATE RE-EMITTED (rear USB-C, run body_demo --hold 5, confirm step [3] antennas now DROOP DOWN and HOLD). U8 stays in_progress. Next: on PASS, mark U8 done then U10 (Phase 2 integration capstone, hardware-gated). NB for U10/live op: outside the demo, the renderer doesn't keep the body "active", so idle breathing will still revert a held signal pose after ~1s — decide there whether to suppress breathing while signalling or accept a momentary glance.
2026-06-29 · U8 · CONTINUE · Body renders FleetState — HUMAN CONFIRMED PASS on the refreshed body_demo: "Pass, all looks good." Body turns toward the flagged manager (two opposite directions at steps 2 & 3), antennas droop clearly DOWN on failure and HOLD, turn acceptably smooth. U8 marked done; pending_gate cleared; units_done 8→9. Phase-2 software is fully shipped; only U10's on-robot end-to-end remains gated. Next: U10 Phase 2 integration capstone (deps U5,U6,U7,U8,U9 all done).
2026-06-29 · U10 · HUMAN_GATE · Phase 2 integration capstone. SOFTWARE integration test green: tests/fleet/test_integration.py — (1) one synthetic FleetSnapshot rendered by CLI (_snapshot_dict), dashboard (snapshot_payload + HTML) and body (FleetBodyRenderer/body_signal_for) all AGREE on who needs attention + colors (dashboard JSON == fleet status --json, no drift); (2) idle-breathing decision at the software seam; (3) a REAL `claude --bg` session driven through ONE FleetState → seen via CLI + dashboard + body at once, then stop_and_remove (0 stray agents). Idle-breathing DECISION (carried from U8 note): do NOT suppress breathing continuously (a frozen robot reads as "dead"; breathing is core to the persona) — accept the momentary breathing glance for steady states, and linger only on attention/failure via a new optional FleetBodyRenderer(hold=...) hook fired when a pose .wants_attention (the live wiring can re-assert the pose briefly, e.g. body_demo.hold_pose on a worker thread). Documented in body.py + plan.md §4 Phase 2 + STATE.yaml. Docs updated: README "What it is" + plan.md Phase-2 header now say the fleet core is built+tested with the on-robot E2E as the last gate. Fleet suite 176 green; body.py/test_integration.py ruff+mypy clean; 0 stray bg agents. HUMAN_GATE: on-robot end-to-end (rear USB-C; run.ps1; open /fleet dashboard; VOICE-spawn a manager; confirm it appears on BOTH body AND dashboard and they agree) — cannot self-verify; steps in STATE.yaml status.pending_gate. U10 stays in_progress. Next: on PASS, mark U10 done → Phase 2 COMPLETE → Phase 3 (U11 run_mode knob, dep U3 done).
2026-06-29 · U10 · HUMAN_GATE · Phase 2 integration capstone — FIX after reported PROBLEM ("robot interacts with a repo but I see NOTHING in the dashboard at /fleet"). ROOT CAUSE = integration wiring the isolated component tests never exercised, TWO disconnects: (a) the RUNNING app (main.run, what run.ps1 launches) built the voice/gradio app but never mounted the /fleet dashboard nor started a FleetPoller, so /fleet 404'd and no FleetState was ever refreshed; (b) the only voice coding tool, ask_claude_code, runs an IN-PROCESS WorkerSession (claude_brain.py) which is NOT a `claude --bg` agent and so never appears in `claude agents --json` — the exact roster the poller reads. Voice-spawn and fleet-observability were not wired together. FIX: new fleet/runtime.py = process-wide FleetRuntime (shared FleetState + SessionManager + started FleetPoller scoped to background managers) + mount_fleet_dashboard() + get/set_fleet_runtime singleton; main.run now calls _mount_fleet(settings_app) (headless, the robot path) and _mount_fleet(app) before gradio's "/" mount, and stops the poller on shutdown; NEW voice tool `spawn_manager` (profiles/_reachy_fleet_supervisor_locked_profile/spawn_manager.py + tools.txt + instructions.txt) routes spawns through runtime.session_manager.spawn → a `claude --bg` manager that IS roster-visible → poller → shared FleetState → dashboard card + body. ask_claude_code kept as the quick in-process delegate (intentionally not fleet-visible); persona now distinguishes "spawn/start a manager" (spawn_manager, watchable) from a quick task (ask_claude_code). Tests: tests/fleet/test_app_wiring.py (5) — mount serves /fleet over the shared state without clobbering settings_app's "/"; build_fleet_runtime poller keeps only background managers; get_fleet_runtime is a started singleton; spawn_manager → fake roster → FleetState → dashboard end-to-end; bad-path rejection. Fleet suite 181 green; main.py + fleet.runtime import-clean; 0 stray bg agents (claude agents --json shows no background sessions after the real-bg integration test cleaned up). plan.md §4 Phase-2 updated. RESIDUAL (only the robot can confirm): the live browser at http://127.0.0.1:7860/fleet showing the card while the body agrees → gate RE-EMITTED with refreshed steps (explicitly say "spawn a manager"; software sanity check `claude agents --json` shows a background "tester"). KNOWN next-iteration wire flagged in pending_gate: FleetBodyRenderer is not yet attached to runtime.state in main.run, so on the robot the body is still driven by openai_realtime status, not FleetState — attach it if the body-half is reported wrong. U10 stays in_progress. Next: on PASS, mark U10 done → Phase 2 COMPLETE → Phase 3 (U11).
2026-06-29 · U10 · HUMAN_GATE · Phase 2 integration capstone — FIX after two reported RENDERING bugs (the human ran the live dashboard; manager cards DID show with names/colors/states/live timestamp, but two display defects). BUG 1 GARBLED TEXT: card last-line + transcript ring buffer showed raw VT/ANSI literally ([38;2;255;107;128m, [K, [200C, [48;3H, [?25h, [m; □ = ESC 0x1b) because `claude logs <id>` returns raw TUI output (SGR colours + cursor/erase/private-mode control seqs) and we stored/rendered it verbatim. FIX: shared sanitizer in fleet/state.py — strip_ansi() (CSI/OSC/two-char escape regex + stray C0 control bytes, keep TAB) + sanitize_lines() (strip each line, DROP lines empty after stripping, e.g. pure `[K`), applied at INGESTION in FleetState.apply() (the one chokepoint feeding the transcript ring buffer) so EVERY renderer — dashboard, CLI, body headline — gets clean text; exported strip_ansi/sanitize_lines. BUG 2 DISCLOSURE AUTO-CLOSES: the transcript is in <details>; the live poll did grid.innerHTML = …map(card) every poll_ms, rebuilding the DOM and resetting <details open>→closed + scroll, so an opened transcript snapped shut on the next ~2s poll. FIX: fleet/dashboard.py served JS now captureUi(grid) (per-card open + pre.scrollTop, keyed by stable data-key) BEFORE the innerHTML rebuild and restoreUi(grid, ui) AFTER, preserving disclosure + scroll across polls. Tests: tests/fleet/test_state.py +6 (strip_ansi SGR/cursor/erase/private-mode/lone-ESC, pure-escape→empty, sanitize_lines drops blanks, apply() sanitizes at ingestion incl. headline) and tests/fleet/test_dashboard.py +2 (card+JSON transcript sanitized & no escape noise; served JS structurally has capture/restore keyed by data-key, restore after innerHTML — JS runtime behaviour is the human's visual gate). Fleet suite 189 green; 0 stray bg agents. NOTE: raw `claude logs` TUI content is inherently noisy; the cleaner long-term signal is each manager's emitted status.json (U5) — sanitizing is the right fix for now. RESIDUAL (human-only): restart the app to load the new dashboard code, then confirm in a browser that card text is CLEAN and an opened "▼ transcript" STAYS open across updates. Gate RE-EMITTED. U10 stays in_progress.
2026-06-29 · U10 · HUMAN_GATE · Phase 2 integration capstone — FIX after reported CRITICAL DEADLOCK (human ran the live fleet: voice-spawned managers got stuck — a `claude --bg` manager showed state BLOCKED / waitingFor "permission prompt" and its raw transcript showed Claude Code's interactive permission UI "bypass permissions on (shift+tab to cycle)"). ROOT CAUSE: a background agent has NO TTY, so the first tool call needing approval blocks it forever, and nothing can clear it (dashboard read-only U7; approve/steer = U12, not built). The manager/session_manager/CLI layers already defaulted to `--permission-mode bypassPermissions`, but the VOICE path (spawn_manager) neither passed nor resolved it explicitly/configurably and the value+behavior had never been verified end-to-end. FIX: (manager.py) VALID_PERMISSION_MODES — verified against `claude --help` on Claude Code 2.1.196: acceptEdits/auto/bypassPermissions/default/dontAsk/plan — + validate_permission_mode() + resolve_permission_mode(override > $FLEET_PERMISSION_MODE > DEFAULT=bypassPermissions); build_spawn_argv now validates the mode. (config.py) fleet-wide default `[fleet].permission_mode` (=bypassPermissions, validated) + per-project `defaults.permission_mode` override (validated) + FleetConfig.permission_mode_for(project). (spawn_manager voice tool) optional `permission_mode` arg in the schema (enum), resolved via resolve_permission_mode and passed EXPLICITLY to SessionManager.spawn; an invalid mode is reported (spoken back), never spawned. CHOSEN DEFAULT = bypassPermissions — trusted LOCAL autonomous managers that escalate real blockers as a HUMAN_GATE in FleetState, not via a dead TUI prompt. SECURITY: bypassPermissions SKIPS ALL permission checks for that session (file/shell/network) — documented in plan.md decision #22 + the gate note; switchable to a gated mode via config/env/arg (acceptEdits then blocks on shell prompts until U12). INTEGRATION-VERIFIED REAL: spawned a `claude --bg` manager with bypassPermissions on a task needing a file-write + shell command — it ran working→done and NEVER showed waitingFor a permission prompt; concurrently observed the human's two leftover managers (no bypass) BLOCKED/waitingFor "permission prompt" (the reported bug). Cleaned up my test agent + the two stray deadlocked ones; `claude agents --json` shows 0 background sessions. Tests: +5 test_manager.py (default is non-interactive; build_spawn_argv rejects invalid mode; validate/resolve precedence incl. env), +6 test_config.py (fleet default + per-project override via permission_mode_for + invalid-mode rejection on both fields), +4 test_app_wiring.py (voice tool defaults to bypassPermissions, explicit override wins, $FLEET_PERMISSION_MODE sets default, invalid mode rejected pre-spawn); `uv run pytest tests/fleet` 203 green. RESIDUAL (only the robot can confirm): restart run.ps1, voice-spawn a manager, confirm its card PROGRESSES (WORKING→done) instead of BLOCKED "waiting: permission prompt", and `claude agents --json` shows it NOT waitingFor a permission prompt (prior clean-text + transcript-stays-open + body/dashboard-agree checks retained). Gate RE-EMITTED. U10 stays in_progress. Next: on PASS, mark U10 done → Phase 2 COMPLETE → Phase 3 (U11).
2026-06-29 · U10 · CONTINUE · Phase 2 integration capstone — HUMAN CONFIRMED PASS on the live robot end-to-end ("pass"): all three reported defects verified fixed on the running fleet — voice-spawned managers no longer DEADLOCK on the interactive permission prompt (they progress WORKING→done with bypassPermissions), transcript text is CLEAN/legible (ANSI sanitized), the "▼ transcript" disclosure STAYS OPEN across the ~2s polls, and the robot BODY + the /fleet DASHBOARD AGREE on who needs attention and what state it's in. U10 marked done; pending_gate cleared; units_done 9→10; current_phase 2→3. **Phase 2 (fleet core + observability + persistence) is COMPLETE — U1–U10 all done.** Next: U11 (Phase 3 — per-manager run_mode knob: background | remote-control; dep U3 done).
2026-06-29 · U11 · HUMAN_GATE · run_mode knob (background | remote-control) — decision #19. SOFTWARE COMPLETE + GREEN (`uv run pytest tests/fleet` 225 passed, +22 over U10's 203; ruff/mypy clean on changed files; 0 stray bg agents). manager.py: RunMode/VALID_RUN_MODES/DEFAULT_RUN_MODE + validate_run_mode + resolve_run_mode (override > $FLEET_RUN_MODE > background); build_spawn_argv gains run_mode and branches background (`claude --bg …`) vs remote-control (`claude --remote-control <name> --session-id … --permission-mode … <task>`, NO --bg); NEW build_remote_control_server_argv (`claude remote-control --spawn {same-dir|worktree|session} --capacity N --name --permission-mode --remote-control-session-name-prefix`). BOTH RC forms verified against Claude Code 2.1.196 `claude --help` + `claude remote-control --help` (NOT invented). FleetManager gains run_mode + an attached detached process; FleetManager.spawn(run_mode=…) dispatches to NEW spawn_remote_control (assembles the RC argv, launches it detached/non-blocking via an INJECTABLE launcher so command assembly is software-testable without a live claude.ai session); RC is not roster-visible (info()→None; is_running tracks the process; stop()/stop_and_remove() terminate the process, never `claude stop`). Wired run_mode through SessionManager.spawn, config (FleetConfig.run_mode default + ProjectDefaults.run_mode now Optional[None]=inherit, mirroring permission_mode; run_mode_for(project)), the `fleet spawn --run-mode` CLI (+ run_mode in JSON/text), and the spawn_manager voice tool (optional run_mode arg; remote-control reply explains it's claude.ai/phone-steerable and NOT on the dashboard). plan.md decision #19 annotated with the verified flags + the U11 implementation. Tests: +12 test_manager (run_mode argv both forms, validate/resolve precedence+env, server-mode argv + invalid spawn/capacity, RC spawn via injected launcher, dispatch, not-roster-visible, is_running tracks process, stop terminates no-CLI), +5 test_config (fleet run_mode default/custom/invalid + run_mode_for override/inherit), +3 test_app_wiring (voice defaults background, remote-control flows + reply, invalid run_mode rejected), +2 test_cli (--run-mode flows + default). RESIDUAL = RC LIVE-CONNECT only: it needs FULL claude.ai OAuth (not the Max coding token) + a live steering session, so it cannot be self-verified → HUMAN_GATE (copy-paste steps in STATE.yaml status.pending_gate: sign in to claude.ai, `claude --remote-control fleet-rc-test …`, confirm it appears + steerable from claude.ai/mobile; SKIP is acceptable — software is complete). U11 stays in_progress. Next: on PASS or "skip RC", mark U11 done → U12 (interactive controls: approve/send/pause/spawn-kill).
2026-06-30 · U11 · CONTINUE · run_mode knob — CLOSED as software-complete: human explicitly chose to SKIP the optional RC live-connect (claude.ai-OAuth steering) check. The RC software path (run_mode knob + background backbone fully wired + RC command assembly verified vs Claude Code 2.1.196 `--help`) is complete and green (tests/fleet 225 passed); the live claude.ai/mobile steering test is deferred, not required for the unit. U11 marked done; pending_gate cleared; units_done 10→11. Next: U12 (interactive controls — approve gate / send message / pause-resume / spawn-kill / reassign).
2026-06-30 · U12 · CONTINUE · Interactive controls (CLI + dashboard) — the write side of the fleet. NEW fleet/control.py: FleetController over SessionManager turns each action into a JSON-ready ControlResult (never raises for expected errors). One shared impl backs both the headless CLI and the dashboard endpoint. Actions: send (= send message / approve-answer a gate / reassign — same call, differ only in what you say), pause (`claude stop`), resume (`claude respawn`), kill (`claude stop`+`claude rm`); remote-control managers report "steer from claude.ai", not the shell. DECISION #16 STEERING MECHANISM RESOLVED + DOCUMENTED: in-place steer of a LIVE bg manager is `claude stop <id>` FIRST (hands the session back from the daemon — a live bg agent REJECTS `--resume … -p` with "currently running as a background agent … add --fork-session"), THEN `claude --resume <sessionId> -p <message>` continues its OWN durable conversation (build_steer_argv). Chose in-place (stop-then-resume) over the non-disruptive `--resume --fork-session -p` because the control's intent is steering the SAME manager, not branching a copy; both documented in control.py + manager.send_message + build_steer_argv. Wired through cli.py (control subcommands), dashboard.py (POST control endpoint), runtime/session_manager. Tests: NEW test_control.py + extended test_manager/test_cli/test_dashboard incl. a REAL bg steering round-trip (spawn → settle → stop-then-resume steer gets a reply → torn down). `uv run pytest tests/fleet` 261 passed; 0 stray bg agents (only the human's interactive reacher-7f remains). U12 done; units_done 11→12. Next: U13 (managers run ralph/drive loops — decision #15).
2026-06-30 · U13 · CONTINUE · Managers run ralph/drive loops — decision #15. NEW fleet/drive.py: DriveLoopSpec(name/repo/command/sentinel_prefix/max_iterations/status_dir/extra_instructions; validated; status_path()) + PURE build_drive_task (the `claude --bg` prompt that turns a manager into a drive-loop orchestrator: run the /drive-* command once per iteration on the target repo, and after EVERY iteration emit its report to EXACTLY its status.json path — baked into the prompt, not env-dependent since bg sessions don't reliably inherit the shell env — escalating HUMAN_GATE then STOPPING rather than looping past a blocker) + spawn_drive_manager (thin wrapper over SessionManager.spawn, name=spec.name, cwd=repo). The sentinel reaches FleetState two ways: (1) the manager's emitted status.json (U5/decision #21 primary path — already read by FleetPoller.status_dir + read_statuses_for_agents) and (2) a NEW FleetPoller fallback `derive_status_from_transcript` (off by default; live runtime enables it when fetch_logs) backed by NEW status.status_from_transcript() which parses the `<PREFIX>: <STATE>` sentinel straight off the `claude logs` transcript tail — so a HUMAN_GATE still surfaces even before/without the on-disk report; the emitted status.json always wins. Surfaced as a NEW `fleet drive --name --repo [--command --max-iterations --sentinel-prefix --run-mode --permission-mode --model]` CLI command. Exported DriveLoopSpec/build_drive_task/spawn_drive_manager/status_from_transcript/DEFAULT_DRIVE_COMMAND/DEFAULT_SENTINEL_PREFIX. Tests: NEW tests/fleet/test_drive.py (spec validation, build_drive_task contents+bounded+extra, spawn wrapper via fake SessionManager) + test_status.py (status_from_transcript + 3 poller-fallback tests incl. status.json-wins + off-by-default) + test_cli.py (2 `fleet drive` tests) + a REAL end-to-end: spawn_drive_manager drives a TRIVIAL sample repo (STATE.txt + DRIVE.md gating on the physical robot) for ONE iteration → manager writes status.json HUMAN_GATE → poll lands it in FleetState (needs_attention) — bounded ~180s ceiling, passed in ~24s, session always stop+rm. `uv run pytest tests/fleet` 273 green (272 + the real-bg integration test run separately); drive.py ruff+mypy clean; `claude agents --json` shows 0 stray bg agents (only the human's interactive reacher-7f). plan.md decision #15 annotated with the U13 implementation. Next: U14 (gate policy — config default + per-worker override; decision #13; deps U1,U13 done).
2026-06-30 · U14 · CONTINUE · Gate policy (config default + per-worker override) — decision #13. Made GatePolicy real (config.py): GatePolicy.escalates(trigger)/classify(trigger) (autonomous mode escalates only escalate_on triggers, case-insensitive; gated mode escalates every non-empty trigger; blank trigger = no gate). Module-level classify_status(policy, status) plugs the REAL gate signal (ManagerStatus sentinel) into the policy: FAILED always escalates (not auto-approvable); HUMAN_GATE labelled via extra["gate_trigger"] follows the policy, a bare gate always escalates; RUNNING/CONTINUE/COMPLETE = autonomous. gate_trigger_of(status) reads the label. Precedence mirrors run_mode/permission_mode: gate_policy_for(name, override=) = explicit per-worker > project override > fleet default; classify_for / classify_status_for resolve+classify. GateDecision/GATE_AUTONOMOUS/GATE_ESCALATE/GATE_TRIGGER_KEY exported. 10 new tests in test_config.py; fleet suite 283 green; 0 stray bg agents (only human's interactive reacher-7f remains). Next: U15 Gate→voice escalation (HARDWARE — deps U13,U14 done).
2026-06-30 · U15 · HUMAN_GATE · Gate → voice escalation (decision #17; HARDWARE; deps U13,U14 done). Made the robot's VOICE a renderer of FleetState alongside the body (one source of truth, many renderers): NEW fleet/voice.py FleetVoiceRenderer — an edge-triggered FleetState subscriber (mirrors FleetBodyRenderer) that SPEAKS on state CHANGES the human cares about: task COMPLETION (explicitly requested: speak on completion, not just gates), build/test FAILED, and HUMAN_GATE "needs you". PURE layer: speakable_kind(m, policy) classifies one ManagerSnapshot → VOICE_FAILED/VOICE_GATE/VOICE_COMPLETED/None, REUSING U14 GatePolicy/classify_status so an AUTONOMOUS (auto-approvable) gate stays silent while an ESCALATING one speaks (bare gate speaks by default; FAILED/COMPLETE always); prefers the emitted report sentinel, falls back to the roster state — same precedence as body.signal_state. voice_phrase_for(kind, m) renders a terse, name-first spoken line folding in the gate/summary first line. The renderer PRIMES SILENTLY on the first snapshot (no stale-backlog blurt on restart; announce_initial opt-in), speaks edge-triggered (a manager that stays failed is announced once), and logs-not-raises on a bad speak callback. SPEAK SEAM: OpenaiRealtimeHandler.announce_fleet_event (injects a [Fleet update] user item + asks the model to relay it briefly aloud — modeled on send_idle_signal) + speak_threadsafe bridge (the FleetPoller notifies subscribers from a WORKER THREAD, so it schedules the coroutine onto the realtime loop captured in _run_realtime_session via run_coroutine_threadsafe; drops cleanly when no live session). Wired in main.py: _mount_fleet attaches a FleetVoiceRenderer(speak=handler.speak_threadsafe) to the shared fleet_runtime.state (best-effort; voice failure never breaks the fleet). RESUME-ON-ANSWER path (the spoken answer resumes the loop): NEW answer_gate voice tool (profile) → FleetController.send (claude --resume <id> -p, U12/decision #16) so the user's spoken reply continues the gated manager's own conversation; registered in tools.txt + concise persona instructions (relay [Fleet update] notes aloud; on a spoken answer call answer_gate). Tests: NEW tests/fleet/test_voice.py (20: speakable_kind for failed/escalating-gate/completed + silent-on-running + autonomous-gate-silent + gated-mode + report/roster fallbacks; voice_phrase_for name+reason; renderer primes-silently/speaks-completion+failure+gate/edge-triggered-no-repeat/announce_initial/survives-bad-speak/drives-off-real-FleetState/policy-driven-escalation off FleetState; answer_gate routes the spoken answer through FleetController.send + requires name+answer). `uv run pytest tests/fleet` 303 green (+20 over U14's 283); 0 stray bg agents (only the human's interactive reacher-7f remains; no real bg sessions spawned — voice unit is fully fake-driven). plan.md decision #17 satisfied. HARDWARE gate: audio/voice needs the physical robot → HUMAN_GATE (copy-paste steps in STATE.yaml status.pending_gate: rear USB-C; run.ps1; TEST A spawn a manager → Reachy SPEAKS completion; TEST B a failing/gated manager → Reachy SPEAKS "needs you" → answer by voice → answer_gate resumes the loop; cleanup + confirm no stray agents). U15 stays in_progress; units_done unchanged (14). Next: on PASS mark U15 done → U16 (written plan file per manager).
2026-06-30 · U15 · HUMAN_GATE · PROBLEM-FIX pass after human live test (still in_progress, re-raised gate). Three fixes, fleet suite 319 green (+16), 0 stray bg agents (only human's interactive reacher-7f). (1) MISSED VOICE GATE — root cause: fleet/voice.py speakable_kind only spoke a gate off the manager's EMITTED HUMAN_GATE sentinel; a manager that merely BLOCKS waiting on the human (claude agents --json state=blocked / waitingFor set, no sentinel — e.g. Claude asked a clarifying question and parked) returned None → nothing spoken and no FleetState gate transition surfaced to the voice. FIX: speakable_kind now also maps a generic blocked/waiting roster row (raw state in blocked/waiting OR waiting_for set) to VOICE_GATE — mirroring ManagerSnapshot.needs_attention + body.signal_state so body and voice agree — and _gate_detail/voice_phrase_for fold in the waitingFor text as the spoken reason. Verified END-TO-END through the REAL FleetPoller→FleetState→FleetVoiceRenderer chain (new test_integration reproduction) + a bounded REAL claude --bg probe (working→done spoke "greeter finished its task"; blocked path unit+integration covered). answer_gate/FleetController.send resume path unchanged (already correct). (2) DISAPPEARING DASHBOARD CARD + missed gate — SHARED root cause: a manager that emits its gate/completion then EXITS drops out of the DEFAULT `claude agents --json` roster, so it vanished from FleetState (card gone AND no terminal snapshot for the voice to speak). FIX: build_fleet_runtime now polls with include_all=True so finished/gated bg managers are retained (their on-disk status.json + log tail still resolve real state); interactive sessions still filtered by predicate; `claude rm` clears a finished one. Dashboard: grid columns widened (minmax min(100%,400px)) + taller wrapped transcript pre; NEW regression test asserts a gated/blocked manager still renders as its own card. (3) MOTION JERK (U8 human ask): NEW pure ease_in_out smoothstep (3t²−2t³, zero end-point velocity) in fleet/body.py, applied in GotoQueueMove.evaluate (was raw linear → snap start/stop), and make_goto_applier duration 1.0→1.6s for a gentler glide; easing math unit-tested (endpoints exact, clamped, monotonic, bounded/no-overshoot, reaches target, symmetric). Tests added: test_voice (+6 roster-gate), test_body (+7 easing), test_dashboard (+2 card-retention/width), test_app_wiring (+1 include_all), test_integration (+1 poller→voice reproduction). Re-raised STATE.yaml pending_gate with refreshed TEST A (completion + card stays) / TEST B (blocked-question gate + answer-by-voice resume) / TEST C (dashboard cards visible+wide+expandable, motion smooth). U15 stays in_progress; units_done unchanged (14).
2026-07-01 · U15 · HUMAN_GATE · gate→voice escalation — PROBLEM-FIX PASS #2 (human live re-test): FOUR fixes in one commit, still hardware-gated. (P1) MOTION TOO BIG, esp. idle: pulled idle-breathing amplitudes out of moves.BreathingMove into NAMED constants and cut them — BREATHING_ANTENNA_SWAY_DEG 15→4, BREATHING_Z_AMPLITUDE_M 0.005→0.002; body signalling shrunk — BODY_YAW_SPAN_DEG 40→18, ANTENNA_POSES ~halved (attention 45→20 / working 15→7 / done 30→13 / failed −45→−20), ANTENNA_MAX_DEG 60→30 + new ANTENNA_SIGNAL_MAX_DEG=22 budget; easing/smoothness kept (magnitude, not smoothness). (P2) DASHBOARD CARDS NOT WIDE: #grid was auto-fill minmax(400px) → replaced with a single full-width column minmax(0,1fr), 2 cols only ≥1400px (≥640px each), body max-width:none; verified against the actually-served HTML (auto-fill/400px assertions gone). (P3) REACHY CUT PEOPLE OFF: refactored inline turn_detection into pure build_turn_detection() (server_vad) with named constants TURN_SILENCE_DURATION_MS ~200→1000, TURN_PREFIX_PADDING_MS→400. (P4) WRONG GATE PHRASING (intermittent "waiting on an action" for a question): new voice.gate_reason_category() (question|approval|generic); voice_phrase_for says "needs your answer" (question) / "needs your approval" (approval) / neutral "needs you" when reason unknown YET — never a wrong category; renderer edge-trigger signature folds in the category so a neutrally-spoken gate RE-announces with the question once waitingFor lands. Tests added: test_body +3 (small yaw band, antenna budget, gentle idle amplitude), test_dashboard net +1 (full-width CSS on served HTML + renders-with-managers), test_voice +5 (question/approval/neutral phrasing, categorizer, neutral→question upgrade), test_openai_realtime +2 (long silence threshold + tunable). Fleet suite 328 green (affected-files run 102 passed); 0 stray bg agents (only interactive sessions present). Re-raised pending_gate with refreshed TEST A/B(question-answer)/C(subtle idle+wide cards)/D(waits-longer). U15 stays in_progress; units_done unchanged (14).
2026-07-01 · U15 · CONTINUE · gate→voice escalation — HUMAN CONFIRMED PASS: all four live-robot tests A–D passed (completion spoken + card retained; gate spoken with correct question/answer phrasing and voice-resume worked via answer_gate; idle motion small/calm + gestures subtle + dashboard cards wide/expandable; Reachy waited through a mid-sentence pause before responding). U15 marked done; pending_gate cleared from STATE.yaml; units_done 14→15. Phase 3 gate→voice escalation (decision #17) is fully shipped. Next: U16 (written plan file per manager; deps [U13] done; software gate).
2026-07-02 · U16 · CONTINUE · Written plan file per manager (decision #11 "optional written plan file per worker for plan-gated autonomy"; deps [U13] done; software gate). fleet/drive.py: DriveLoopSpec gains plan_content (optional plan.md-style task+gates text) + plan_path (default "plan.md", resolved relative to repo unless absolute via new resolved_plan_path(); validated non-empty). NEW write_plan_file(spec, overwrite=False): no-op when plan_content is None; otherwise creates parents + writes the plan file — but NEVER clobbers an existing file by default (a manager's own committed plan edits are the drive contract's durable state and must survive a respawn/reconnect with the same spec; overwrite=True is the explicit escape hatch). build_drive_task gains a plan clause: when plan_content is set, the prompt names the EXACT plan path and instructs the manager to treat it as the authoritative committed state for its task+gates and to keep it updated per iteration; when None the clause is omitted → prompts for spec-without-plan are byte-compatible with U13 (all prior tests pass unchanged). spawn_drive_manager materializes the plan via write_plan_file BEFORE spawning so the file exists on the manager's first read. write_plan_file exported through fleet/__init__. 9 new tests in tests/fleet/test_drive.py (plan defaults None, plan_path validation, relative+absolute path resolution, no-content no-op, write, no-clobber + overwrite escape hatch, prompt clause present/absent, spawn materializes file + prompt references it). Full fleet suite 337 green (+9 over U15's 328). 0 stray bg agents at end — note: an earlier timeout-bounded partial pytest run in this iteration killed tests/fleet mid-flight and orphaned one u12-steer bg agent (7ef8bd91); found via claude agents --json and stopped/removed. Lesson: don't wrap the fleet suite in a shell timeout shorter than its real-bg tests' teardown. Next: U17 (voice interaction contract; deps U11,U14 done; HARDWARE gate).
2026-07-02 · U17 · HUMAN_GATE · Voice interaction contract (decision #20): finished + committed a stalled prior worker's partial work. fleet/voice_contract.py (pure, tested): wants_immediate_kickoff (matches "just do it"/"skip the confirmation"/... case-insensitive substrings), sounds_like_confirmation ("go ahead"/"yes"/"do it"/...), summarize_spawn_confirmation (short spoken readback: task+location always, run_mode/permission_mode/gate_note only when non-default), EXPLAIN_OPTIONS_TEXT (spoken options + example). spawn_manager.py tool gained a confirmed:boolean param (decision #20): first call (confirmed omitted/false) returns status="needs_confirmation" + the spoken confirmation text WITHOUT spawning; a second call with confirmed=true (or an up-front "just do it") actually spawns. Persona instructions.txt updated with the confirm-before-kickoff flow, the skip phrase, and how to explain options by voice. Added tests/fleet/test_voice_contract.py (13 tests) + tests/tools/test_spawn_manager.py (3 tests: no-spawn-without-confirmed, spawn-with-confirmed=true, confirmed=false still gates) and fixed 6 tests/fleet/test_app_wiring.py voice_spawn tests that called the tool expecting immediate spawn (now pass confirmed=True). Full suite: fleet 347 green, tools 36 green (2 pre-existing unrelated profile-loading failures confirmed present on main before this change — torch-less vision + tools.txt-not-found, untouched). 0 stray bg agents (no real claude --bg spawned this iteration). Still HARDWARE-gated: whether the readback sounds natural and the confirm/skip/explain paths work by voice on the robot needs a human live test. pending_gate script below. Next after PASS: U18 MCP-general per-worker MCP config.
2026-07-02 · U18 · CONTINUE · MCP-general per-worker MCP config (deps [U1,U2] done; software gate). U17's HUMAN_GATE deferred by explicit human choice (continue on software units); pending_gate left untouched. fleet/config.py gains pure helpers: mcp_server_entry(McpServerConfig) -> the claude --mcp-config entry shape ({command,[args],[env]} for stdio, {type:"http",url,[env]} for URL servers), mcp_config_payload(servers) -> {"mcpServers": {name: entry, ...}}, write_mcp_config_file(servers, path) -> materializes the JSON file (creating parent dirs) or returns None untouched for an empty list ("config only, no hard dependency" per acceptance), and ProjectConfig.materialize_mcp_config(path) wrapping it for a project's own mcp[]. fleet/session_manager.py gains SessionManager.spawn_project(fleet_config, project_name, task, name=...): resolves cwd from the project path and run_mode/permission_mode/model from the project's effective config (reusing U11/U14's run_mode_for/permission_mode_for/ProjectDefaults.model precedence), materializes the project's mcp[] to <tmp>/reachy-fleet-mcp/<project>.mcp.json (stable path across repeat spawns, overridable via mcp_config_dir), and threads it into FleetManager.spawn's existing mcp_configs=[...] (U2's --mcp-config plumbing, unchanged); extra_mcp_configs layers additional files on top. Exported mcp_server_entry/mcp_config_payload/write_mcp_config_file via fleet/__init__. 18 new tests: tests/fleet/test_config.py (+8: stdio/http entry shapes, payload shape incl. empty, write_mcp_config_file empty-noop and file-write+dir-creation, ProjectConfig.materialize_mcp_config empty/non-empty) and tests/fleet/test_session_manager.py (+2: spawn_project resolves cwd/run_mode/permission_mode/model + materializes+wires the mcp file with correct JSON content; a project with no mcp[] passes an empty mcp_configs list). No real claude --bg spawned by these tests (all mocked via monkeypatch on FleetManager.spawn, per existing test_session_manager.py style). Full fleet suite 355 green (+18 over U17's 347; tests/vision remains a separate, pre-existing torch-less collection error, unrelated and untouched). Found and cleaned one stray u12-steer bg agent (e2f7f769) left over from an earlier iteration's test run, via `claude agents --json` + stop/rm — 0 stray agents at end. units_done 16→17. Next: U19 (pluggable coding backend / local Ollama; deps [U2] done; software gate) — U17's voice-interaction-contract HUMAN_GATE remains open, pending a live robot test.
2026-07-02 · U19 · CONTINUE · Pluggable coding backend (Ollama local model) — decision #14, option 1. fleet/config.py: OllamaBackendConfig (base_url, optional model) + BackendConfig (kind: claude|ollama; ollama block required when kind=ollama) + pure backend_env(backend) (claude -> {}, ollama -> ANTHROPIC_BASE_URL + optional ANTHROPIC_MODEL) + write_backend_settings_file(backend, path) (materializes a project's .claude/settings.json env block; claude backend writes nothing — no override needed; ollama MERGES into any existing settings.json, preserving other keys like permissions, never clobbering). ProjectDefaults gains backend: BackendConfig | None (unset = inherit fleet default, mirroring run_mode/permission_mode); FleetConfig gains backend: BackendConfig default + backend_for(name, override=) precedence (explicit override > project defaults.backend > fleet default); ProjectConfig.materialize_backend_settings thin wrapper. session_manager.py: SessionManager.spawn_project resolves the effective backend via backend_for and materializes it into <project.path>/.claude/settings.json BEFORE spawn (a background `claude --bg` session has no shell/TTY to inherit ANTHROPIC_BASE_URL from — Claude Code 2.1.174+ reads project settings.json env instead), so Claude Code itself gets routed at the local model while keeping its own agent loop/tooling (option 1, not a native Ollama agent). All new symbols exported via fleet/__init__. 16 new tests (backend defaults, ollama-requires-block validation, base_url-not-blank, backend_env both kinds, write_backend_settings_file no-op/create/preserve-existing-keys, ProjectConfig wrapper, FleetConfig.backend_for precedence, spawn_project materializes ollama settings + default writes nothing). `uv run pytest tests/fleet` 367 green (+12 net over U18's 355); 0 stray bg agents (`claude agents --json` showed only the human's 2 interactive sessions, no fleet-spawned background managers). NOTE: local Ollama IS already running on this machine (localhost:11434, models incl. llama3.1:8b) — the acceptance's "if no local Ollama present -> HUMAN_GATE to set it up" branch does not apply; a LIVE spawn-through-Ollama smoke test (does a real claude --bg manager actually complete work when routed at a local model) was judged out of scope for this PR-sized unit (acceptance only requires "config + tests") and is left as a natural follow-up, not a blocking gate. Next: U20 (Vision — worker-callable tool, decision #9; deps U13 done; hardware-gated for the robot-camera path, screen-capture path software-testable).
2026-07-02 · U20 · CONTINUE · Vision — worker-callable tool (decision #9; deps [U13] done; HARDWARE-gated unit, software slice completed + robot-camera verification DEFERRED per explicit human choice 2026-07-02 — recorded in STATE.yaml status.deferred_gates, NOT blocking the loop; U17's voice pending_gate also remains open/untouched). NEW fleet/vision.py — the "look at what I built" loop: capture -> Claude assessment -> text back into the worker's loop. capture_screen (PIL.ImageGrab all-monitors -> downscaled JPEG <=1568px; grabber injectable; Pillow already present transitively) + capture_robot_camera(frame_provider=...) — the physical-camera seam accepts a numpy/PIL frame from the live app, and WITHOUT one raises RobotCameraUnavailable (never fakes a perceptual check, per the drive contract). Assessment reuses the .phase0/vision_ref CLI pattern (Max plan, NO API key): build_assessment_prompt (pure) + assess_image runs one-shot `claude -p <prompt> --allowedTools Read` (injectable runner; timeout/exit/empty-output -> VisionError). look() orchestrates capture->assess -> LookResult{source,image,assessment}, cleaning up temp captures (even on failure) unless --keep/--out. WORKER WIRING (the "worker-callable" part): new `fleet look <question> [--source screen|camera --image --out --keep --timeout --json]` CLI subcommand — a bg manager just runs it via Bash (camera without the robot = exit 2, screen path exit 0/1); look_command(project_dir) builds the `uv run --project <supervisor app> fleet look --source …` argv prefix so the console script resolves from ANY target repo; DriveLoopSpec gains vision_command — when set, build_drive_task advertises the tool + usage in the manager's prompt (omitted when None -> all prior prompts byte-compatible; blank rejected). Exports via fleet/__init__. NEW tests/fleet/test_vision.py — 27 tests: capture (jpeg write, temp file, downscale, RGBA->RGB, grabber-failure, REAL-desktop smoke grab auto-skipping headless), camera gating + injected PIL/numpy frames, prompt/argv shape, CLI error taxonomy (nonzero exit/timeout/missing bin/empty output), look end-to-end (temp cleanup incl. on failure, keep/out/existing-image, camera gated + injected, bad source), worker wiring (look_command shape+validation, drive prompt clause present/absent, blank vision_command rejected), and `fleet look` CLI (prints assessment, --json shape, option passthrough, camera-gated exit 2, VisionError exit 1). Full fleet+tools suites 437 green (+27 over U19's fleet count with tools included; tests/vision torch-less collection error remains pre-existing/unrelated). 0 stray bg agents (`claude agents --json`: only the 2 interactive human sessions; no real bg spawned — vision unit fully fake-driven). U20 marked done (units_done 18->19) with its robot-camera live check parked in deferred_gates (rear USB-C; wire a frame_provider per .phase0/vision_ref/main.py; `fleet look --source camera --keep`; verify real frame + plausible assessment). Next: U21 (Vision — supervisor observer; deps [U20] done; hardware unit — same software-first treatment applies).
2026-07-02 · U21 · CONTINUE · Vision — supervisor observer (decision #9; deps [U20] done; HARDWARE-gated unit, software slice completed + robot verification DEFERRED per the standing human choice 2026-07-02 — recorded in STATE.yaml status.deferred_gates, non-blocking; U17's voice pending_gate remains open/untouched). NEW fleet/observer.py — the proactive half of decision #9 vision: FleetObserver, one more edge-triggered FleetState subscriber mirroring FleetVoiceRenderer exactly (primes silently on the first snapshot so a restart never retro-glances; per-manager kind tracking via voice.speakable_kind so observer + voice agree on state; glance fires ONCE per fresh transition into a glance-worthy kind). Default glance policy = COMPLETION ONLY (DEFAULT_GLANCE_KINDS={completed} — failures/gates are already spoken by the voice renderer and need no visual assessment; glance_kinds is a constructor knob). Pure pieces unit-tested: should_glance (reuses U14 GatePolicy via speakable_kind) + build_glance_question (names the manager + cwd, asks for a one-sentence supervisor verdict) + build_glance_comment (trims a raw assessment to its first line, name-first: "I took a look at <name>'s work — ..."). The glance itself calls an injectable look_fn (default = U20's vision.look, screen source by default; source="camera" supported) and speaks via the injected speak callback; VisionError (incl. the hardware-gated RobotCameraUnavailable) is logged + swallowed = "nothing to glance at this time", NEVER a faked perceptual comment, and a throwing speak callback can't kill the poller (same contract as the body/voice renderers). LIVE WIRING: main.py _mount_fleet attaches FleetObserver(speak=handler.speak_threadsafe) to fleet_runtime.state right after the voice renderer, best-effort (wiring failure logged, never breaks the app). Exports via fleet/__init__ (FleetObserver/GlanceReport/should_glance/build_glance_question/build_glance_comment/DEFAULT_GLANCE_KINDS). NEW tests/fleet/test_observer.py — 19 tests: pure decision/question/comment (completion-only default, custom glance_kinds, cwd in question, first-line trim, empty assessment), subscriber behavior (silent prime, glance+speak on completion w/ question+source captured, failure/gate ignored, edge-trigger no-repeat, announce_initial, per-manager independence, real-FleetState attach/unsub), and error tolerance (VisionError + RobotCameraUnavailable swallowed w/o comment, bad speak callback survives). Full suite (--ignore=tests/vision): 473 passed, 2 failed = the KNOWN pre-existing profile-loading failures (test_config_name_collisions / test_external_loading, documented since U1 — untouched, unrelated). 0 stray bg agents (`claude agents --json`: only the 2 interactive human sessions; observer unit fully fake-driven, no real bg spawned). U21 marked done (units_done 19->20) with its listen-on-the-robot check parked in deferred_gates (rear USB-C; finish a trivial manager; LISTEN for the completion line + the "I took a look at ..." glance comment). Next: U23 (rate-limit + robustness hardening; deps [U3] done; software) — U22 needs the 5090+audio hardware, U24/U25 are Phase 5.
2026-07-02 · U23 · CONTINUE · Rate-limit + robustness hardening for N managers sharing one Max-plan pool (deps [U3] done; software gate). U22 skipped for now (needs the 5090 + audio hardware, not available on this machine) — left pending; U17's voice pending_gate + U20/U21's deferred robot-verification gates remain open/untouched, all per the standing 2026-07-02 human choice to keep making software progress. NEW fleet/resilience.py: is_rate_limit_error(text) — pure, case-insensitive substring classifier over Claude CLI stderr/stdout ("rate limit", "429", "usage limit", "quota exceeded", "overloaded", "capacity", "please try again/retry later", etc.) so a transient pool collision is distinguishable from a genuine task/argv failure. RetryPolicy (max_attempts/base_delay/max_delay/multiplier/jitter, validated in __post_init__) + compute_backoff_delay (pure exponential-with-cap, jitter injectable via `rand` for determinism) + retry_call(fn, policy=, is_retryable=, sleep=, rand=, on_retry=) — a generic retry wrapper: retries ONLY a retryable (rate-limit-shaped) error with backoff between attempts, any other error propagates on the FIRST try (fails fast, unchanged from pre-U23 behavior), and the LAST exception is re-raised once attempts are exhausted. Recovery half: classify_manager_health(agent_or_None) -> "healthy" | "recoverable" (roster still lists it failed/stopped) | "missing" (dropped off the roster entirely — nothing to respawn FROM) + recover_manager(manager, policy=, ...) which respawns a "recoverable" manager via FleetManager.resume() (`claude respawn`), itself run through retry_call so a respawn that ALSO collides with the shared pool's rate limit backs off and retries instead of failing outright. WIRING: SessionManager.spawn and SessionManager.send_message both gained a retry_policy kwarg (default DEFAULT_RETRY_POLICY = enabled; pass retry_policy=None to opt out and get the exact pre-U23 single-attempt behavior) wrapping the underlying FleetManager call in retry_call; SessionManager gained health(key)/recover(key)/recover_all() — recover_all sweeps every tracked manager, logging (not raising) on one manager's recovery failure so it never stops the rest of the sweep. All new symbols exported via fleet/__init__ (RetryPolicy, ManagerHealth, DEFAULT_RETRY_POLICY, RATE_LIMIT_MARKERS, RECOVERABLE_STATES, is_rate_limit_error, is_retryable_exception, compute_backoff_delay, retry_call, classify_manager_health, recover_manager). NEW tests/fleet/test_resilience.py (34 tests: classification, RetryPolicy validation, backoff math incl. jitter bounds + cap, retry_call success/retry-then-succeed/exhaustion/non-retryable-fails-fast/on_retry callback, health classification for all three states, recover_manager no-op/resume/retry-on-rate-limit/propagate-non-retryable) + 12 new tests appended to tests/fleet/test_session_manager.py (spawn retries on rate-limit then succeeds, does NOT retry a non-rate-limit spawn error, retry_policy=None disables retry, send_message retries, health/recover/recover_all wiring) — all fully mocked (monkeypatched FleetManager.spawn/send_message/info/resume), zero real `claude --bg` spawned by this unit's own tests since the point under test is pure retry/backoff LOGIC, not the CLI surface. Full software suite (`uv run pytest --ignore=tests/vision/test_processors.py`, foreground, ~140s): 520 passed, 2 failed = the same known pre-existing profile-loading failures (test_config_name_collisions / test_external_loading, documented since U1 — untouched, unrelated); tests/vision/test_processors.py collection (missing torch) also pre-existing/unrelated, ignored per standing guidance. Found + cleaned one stray u12-steer bg agent (39b20bec) left over from an EARLIER iteration's real-bg test run (not created by this iteration — this unit's own tests are fully mocked) via `claude agents --json` + stop/rm; 0 stray bg agents at end. units_done 20->21. Next: U24 (Customization: keys/models/projects/personas + UI polish; deps [U7,U11] done; software, visual polish = light human check) — Phase 5.
